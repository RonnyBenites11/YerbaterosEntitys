@model IEnumerable<YerbaterosEntitys.Models.tb_Usuario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Correo_Usuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contraseña_Usuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Correo_Usuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contraseña_Usuario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cod_Usuario }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cod_Usuario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cod_Usuario })
        </td>
    </tr>
}

</table>
