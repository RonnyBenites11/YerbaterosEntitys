@model IEnumerable<YerbaterosEntitys.Models.tb_VentaPasaje>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_Venta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Monto_Total)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tb_Usuario.Correo_Usuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tb_DetalleVentaPasaje.Voucher)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_Venta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Monto_Total)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tb_Usuario.Correo_Usuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tb_DetalleVentaPasaje.Voucher)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
